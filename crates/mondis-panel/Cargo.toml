[package]
name = "mondis-panel"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
# GTK
gtk = { package = "gtk4", version = "0.7", features = ["v4_6"] }
glib = "0.18"
regex = "1"
async-channel = "2.0"
